# QAE Macro Dashboard 사용법

> 마지막 갱신: 2026-07-27
> 흐름 한 줄 요약: 입력 파일 갖다놓기 → 폴더별 스크립트 실행(HTML 생성)
> → macro_hub JSON/embeds 갱신 → git push → Vercel 자동 배포

---

## 1. 전체 구조

```
[입력 엑셀/API]
   → 폴더별 BeforeHTML/AfterHTML → 각 폴더 dashboard.html
        ├→ build_master.py       → master_dashboard.html (로컬 통합판)
        └→ macro_hub/scripts/*.py → public/data/*.json + public/embeds/*.html
              → git push → Vercel 자동 재배포
```

- 로컬 확인용: `master_dashboard.html` (더블클릭으로 열기)
- 배포용: `macro_hub` (Next.js) → Vercel 자동 배포

---

## 2. 폴더별 — 어떤 파일을 갖다놔야 하나

### 경제지표가져오기 (경제지표 캘린더)
- 입력: `블벅경제지표\weco_global.xlsx` ← 블룸버그 WECO 내보내기
  (국가별 시트: us / de / ca / au / UK / fr / jp)
- 실행: `python weco_dashboard.py`
- 키: 없음

### releasecalendar (실적 캘린더)
- 입력: `기업실적.xlsx` ← 블룸버그 EVTS 내보내기 (Name / Ticker / Date 필수)
- 실행: `python build_earnings_calendar.py`
- 키: 없음
- 참고: 시총순 정렬은 `marketcap.json` 사용. 새 종목이 엑셀에 추가되면
  이 파일에도 시총 값을 추가해야 함 (없으면 맨 아래 정렬).

### Consensus Builder (CPI/GDP 컨센서스)
- 입력: `ECFC_Growth Consesus_수정.xlsb`, `ECFC_Inflation Consesus_수정.xlsb`
  ← 블룸버그 ECFC 내보내기 (최근 60일치, 기존 파일에 덮어쓰기)
- 실행 (순서대로):
  1. `python merge_xlsb_to_xlsx.py`  → history\에 오늘 날짜 xlsx 생성
  2. `python "CPI consensus.py"`
  3. `python "GDP consensus.py"`
- 키: 없음
- 주의: 60일 넘게 안 돌리면 데이터 갭 생김. 최소 두 달에 한 번은 갱신.

### Consensus Builder (CPI 품목별 분포) — 컨센서스와 별개 소스
- 입력: 없음. **티커 목록만 있으면 최신까지 자동으로 받아온다.**
  (`haver\haver-api_CPI\tickers.xlsx` — 4,486개. git에 올라가 있음)
- 실행 (순서대로):
  1. `cd haver\haver-api_CPI` → `python fetch_haver_to_excel.py`
     → `dashboard_data_CPI Distribution.xlsx` 생성/갱신 (약 2분, 2.5MB)
  2. `cd "Consensus Builder"` → `python "CPI distribution.py"` → `cpi_dashboard.html`
  3. `cd macro_hub` → `python scripts\sync_embeds.py` (웹에 올릴 때만)
- 키: **없음. Haver는 API 키가 없다.** DLX Direct 로그인창에 이메일/비밀번호 →
  이메일로 온 보안코드 입력, 그게 전부. 한 번 로그인하면 한동안 유지됨.
  (`.env`의 DLXPAR / DLXDB는 키가 아니라 *로컬* DLX 데이터 폴더 경로. 선택사항)
- 데이터: 블룸버그 아님. Haver 티커 약 4,500개
  (GERMANY / JAPAN / USECON / EUDATA / FRANCE / UK / CANADA / ANZ / G10),
  국가 분류는 descriptor 콜론 앞부분 기준. 5번째 패널만 G10 DB 티커 하드코딩.
- 증분 수집: `last_dates.json`이 티커별 마지막 수집일을 기억. 다시 돌리면
  그 이후만 받아 붙임. 날짜를 손으로 넣을 일 없음.
- 주의: `last_dates.json`에는 **데이터를 받아온 티커만** 기록됨.
  수집에 실패한 티커는 여기서 사라지므로, 이 파일로 티커 목록을 복원하면
  구멍이 생긴다. 티커 원본은 `tickers.xlsx`(git 추적 중)를 기준으로 볼 것.

### PCA (경기지수 / LEI)
- 입력: `haver\haver-api_PCA\Meta data_Raw data.xlsx`
  ← `haver\haver-api_PCA\fetch_haver_to_excel.py` 실행으로 생성 (Haver DLX 로그인 필요)
- 실행: `python pca_gdp.py`
- 키: 없음 (위 CPI 분포와 동일 — DLX Direct 로그인만)

### gs_api (CAI·MAP + Equity Factors)
- 입력: 없음 (GS API 직접 호출)
- 실행: `python build_cai_map.py` → `python build_pairbaskets.py`
- 키: `gs_api\.env` (GS_CLIENT_ID / GS_CLIENT_SECRET)

### policytone (중앙은행 발언 매파/비둘기)
- 입력: 없음 (웹 수집)
- 실행: `python collect.py` → `python score.py` → `python hawkdove_dashboard.py`
- 키: `policytone\.env` (ANTHROPIC_API_KEY)

### report_pipeline (증권사 리서치 수집·요약)
- 입력: 없음 (셀레니움 수집, 크롬 디버그포트 9222 필요)
- 실행: `python download_reports.py` → `python summarize.py`
- 키: `report_pipeline\.env` (ANTHROPIC_API_KEY 등, 견본 `.env.example`)

---

## 3. 전체를 한번에 업데이트

### 방법 A — `전체업데이트.bat` 더블클릭 (권장)

입력 파일들(위 2번)을 갖다놓은 뒤, QAE 루트의 **`전체업데이트.bat`** 를 더블클릭하면
Haver 수집 → 리포트 수집 → 폴더별 데이터·HTML → JSON/embeds → git push 까지 한 번에 돕니다.
(Vercel이 push 감지 후 1~2분 뒤 자동 재배포)

옵션은 여러 개 같이 써도 됩니다.

| 옵션 | 효과 |
|---|---|
| (없음) | 전부 실행 |
| `/nohaver` | Haver 수집 생략 (DLX 로그인 하기 싫을 때, 기존 엑셀 사용) |
| `/noreport` | 셀레니움 리포트 수집 생략 |
| `/webonly` | 수집·HTML 생성 전부 생략, JSON/embeds만 다시 굽기 |
| `/nopush` | 커밋만 하고 push 안 함 |
| `/nogit` | git을 아예 안 건드림 (로컬만 갱신할 때) |
| `/dataonly` | `git add`를 산출물(`macro_hub\public` 등)로 제한 — 작업 중인 코드가 딸려 올라가는 게 싫을 때 |
| `/dryrun` | 실제로 돌리지 않고 **무엇이 실행될지만** 미리 보기 |
| `/nopause` | 끝에서 키 입력을 안 기다림 (작업 스케줄러 무인 실행용) |

```bat
:: 예) Haver·리포트 없이 나머지만 갱신하고 배포
전체업데이트.bat /nohaver /noreport

:: 옵션 조합이 헷갈리면 먼저 미리보기
전체업데이트.bat /nohaver /dryrun
```

- 한 단계가 실패해도 멈추지 않고 끝까지 진행하며, **맨 끝에 실패한 단계 목록**을 보여줍니다.
- 기본 `git add -A` 라서 작업 중인 코드까지 커밋됩니다. 싫으면 `/dataonly`.

### 실행 이력 — `scheduler_dashboard.html` 에 자동 반영

실제 작업은 `전체업데이트.bat` 이 부르는 **`run_qae.py`** 가 합니다.
이 스크립트가 실행할 때마다 `QAE\logs\` 에 기록을 남기고,
상위 폴더의 `scheduler_dashboard.py` 를 호출해 대시보드를 갱신합니다.

```
QAE\logs\qae_YYYYMMDD.log   그날 실행 상세 로그 (단계별 ▶ ~ ◀ 구간)
QAE\logs\run_history.csv    실행 1건 = 1줄 요약
QAE\logs\run_steps.csv      단계별 소요시간 (대시보드 가로 바차트)
QAE\logs\run_failures.csv   실패한 단계 상세
        ↓
C:\Users\infomax\Documents\python\scheduler_dashboard.html
        → "QAE 대시보드 갱신 (Vercel)" 항목
```

대시보드에서 이 항목을 클릭하면 12개 단계의 소요시간 바차트가 나오고,
단계를 클릭하면 **그 단계의 로그 원문**이 펼쳐집니다.
(CME / 유투브 BBG / 보따리와 같은 화면에서 함께 관리됩니다.)

- 이 로그들은 `.gitignore` 에 있어 QAE 저장소에는 올라가지 않습니다(로컬 전용).
- 실행 예정일은 `python\projects.json` 의 `schedule_hint` 로 표시됩니다.
  작업 스케줄러에 등록하면 `schtasks_name` 에 작업 이름을 적어두세요 — 그러면
  스케줄러에서 실제 다음 실행 시각을 직접 읽어옵니다.

### 방법 B — 손으로 한 줄씩

```bat
cd C:\Users\infomax\Documents\python\QAE

:: 1) 데이터 단계 (Consensus 병합, policytone 수집 등)
python BeforeHTML_master.py

:: 2) HTML 생성 (모든 폴더 + master_dashboard.html)
python AfterHTML_master.py

:: 3) Vercel용 JSON 5종
cd macro_hub
python scripts/build_pairbaskets_json.py
python scripts/build_policy_json.py
python scripts/build_consensus_json.py
python scripts/build_pca_json.py
python scripts/build_caimap_json.py
::  (npm 있으면 위 5줄 대신: npm run data)

:: 4) 자체완결 HTML → embeds 복사 (경제지표·실적캘린더 등)
python scripts/sync_embeds.py
```

- 한 폴더가 실패해도 다음 폴더는 계속 진행됨.
- 키/입력 없는 폴더는 그것만 실패하고 나머지는 정상 갱신됨.
- 위 B는 Haver 수집(PCA·CPI분포 엑셀)과 리포트 수집이 빠져 있음 — 필요하면 따로 실행.

### 매일 자동 실행 (스케줄러)

`_자동화\run_all_0830.bat` 은 이제 한 줄짜리입니다 — 무인 실행용 옵션만 붙여
`전체업데이트.bat` 을 부릅니다.

```bat
call "%~dp0..\전체업데이트.bat" /nohaver /nopause %*
```

- `/nohaver` — Haver는 DLX 로그인창이 떠서 무인 실행이 안 됩니다. 수집은 건너뛰고
  PCA·CPI분포는 기존 엑셀로 다시 그립니다. Haver까지 새로 받으려면 사람이 있을 때
  `전체업데이트.bat` 을 직접 더블클릭하세요.
- `/nopause` — 이게 없으면 스케줄러가 `pause`에서 영원히 멈춰 있습니다.
- 뒤에 옵션을 더 붙이면 그대로 전달됩니다. 먼저 확인하려면 `run_all_0830.bat /dryrun`.

등록은 `_자동화\스케줄등록_0830.bat` 을 **관리자 권한으로 1회** 실행.

> **현재 이 PC에는 등록돼 있지 않습니다** (작업 스케줄러에 `QAE대시보드갱신` 없음).
> 지금은 전부 수동 실행 중입니다.

`projects.json` 의 QAE 항목에는 `schtasks_name: "QAE대시보드갱신"` 을 미리 적어뒀습니다.
등록 전에는 무시되고 `schedule_hint` 글자가 표시되며, 등록하고 나면 대시보드를 만들 때
`schtasks /Query` 로 **실제 다음 실행 시각**을 읽어와 표시합니다.

---

## 4. Vercel 배포

### 방법 A — git push (권장, 평소 쓰는 방법)

Vercel이 GitHub 레포(squarrel808/QAE-dashboard)에 연결돼 있어서
push만 하면 자동 배포됨 (1~2분).

```bat
cd C:\Users\infomax\Documents\python\QAE
git add -A
git commit -m "데이터 갱신 YYYYMMDD"
git push
```

- 배포 상태: vercel.com → 프로젝트 → Deployments
- 특정 폴더만: `git add "Consensus Builder" macro_hub` 처럼 지정

### 방법 B — Vercel CLI (git 없이 강제 배포, 비상용)

```bat
:: 최초 1회만
npm i -g vercel
cd C:\Users\infomax\Documents\python\QAE\macro_hub
vercel login
vercel link

:: 이후 배포할 때마다 (macro_hub 폴더에서!)
vercel --prod
```

- CLI 배포는 git 이력과 어긋날 수 있으니 평소엔 방법 A 사용.

### Vercel 환경변수

- 실시간 시세(LiveQuote)용 `FMP_API_KEY`는
  vercel.com → Settings → Environment Variables 에 등록 (코드/git에 안 넣음).

---

## 5. .env 정리 (git에 안 올라감 — PC마다 직접 준비)

- `gs_api\.env`
  - `GS_CLIENT_ID=...`
  - `GS_CLIENT_SECRET=...`
- `haver\haver-api_PCA\.env`, `haver\haver-api_CPI\.env` — **선택사항 (없어도 됨)**
  - Haver는 API 키가 없다. 인증은 DLX Direct 로그인창(이메일 + 보안코드)뿐.
  - `DLXPAR=...` / `DLXDB=...` 는 *로컬* DLX 데이터 폴더를 쓸 때만.
    DLX Direct(서버 직결)로 받으면 안 넣어도 동작함
  - DB 업로드까지 쓸 때만: `POSTGRE_API_URL=...` / `POSTGRE_API_KEY=...`
- `policytone\.env`
  - `ANTHROPIC_API_KEY=sk-ant-...`
- `report_pipeline\.env`
  - `ANTHROPIC_API_KEY=...` / `CLAUDE_MODEL=...` / `STORAGE=local`
- `macro_hub\.env.local`
  - `FMP_API_KEY=...` (로컬 개발용. 배포는 Vercel 환경변수)

---

## 6. 자주 겪는 문제

- Vercel 반영 안 됨
  → JSON/embeds 갱신 후 git push 했는지, `macro_hub/public/data/*.json` 날짜 확인
- Consensus 그래프에 구멍
  → xlsb 공백 기간. `history\` 최신 파일 기준으로 재병합 필요
- xlsb가 "File is not a zip file" 에러
  → 블룸버그 복사 중 파일 손상. 다시 내보내거나 zip 복구
- PCA 실행 실패
  → 입력 엑셀 없음. Haver 로그인 후 `fetch_haver_to_excel.py` 먼저
- CPI 분포 실행 실패 (`dashboard_data_CPI Distribution.xlsx not found`)
  → `haver\haver-api_CPI\fetch_haver_to_excel.py`를 먼저 돌리면 생김.
    `ModuleNotFoundError: No module named 'Haver'`가 뜨면 패키지부터 설치:
    `pip install Haver --extra-index-url http://www.haver.com/Python --trusted-host www.haver.com`
- CPI 분포에서 특정 국가의 Core CPI 모멘텀 패널(5번째)이 안 나옴
  → 그 국가의 G10 티커가 `tickers.xlsx`에 없는 것.
    `CPI distribution.py`의 `G10_CORE_CPI_MAP` 7개와 대조해볼 것
- gs_api 실패
  → `.env` 없거나 만료. 실행 초반 `[OK] 인증` 메시지 확인
- bat 파일 한글 깨짐
  → `_자동화\*.bat`는 cp949 인코딩으로 저장
- git push 실패
  → 이 PC의 git 자격증명 확인 (자동화/샌드박스 환경엔 자격증명 없음)
