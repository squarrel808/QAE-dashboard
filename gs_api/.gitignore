# ── 비밀/환경 변수 (API 키 등) — 절대 커밋 금지 ──
.env
.env.*
!.env.example
*.key
*.pem
secrets.json
credentials.json
gs clinetid.txt

# ── Python ──
.venv/
venv/
__pycache__/
*.pyc

# ── 로그 (데이터 산출물 outputs/, csv, db 는 이제 git에 올라감) ──
logs/

# ── 에디터/OS ──
.vscode/
.DS_Store
.ipynb_checkpoints/
