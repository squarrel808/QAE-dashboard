# 리서치 PDF 아카이브 — 텔레그램 방식 (보류 중, 나중에 재개)

> 작성일 2026-08-05 · 상태: **1~6단계 사용자 작업 대기 중**
> 재개할 때 Claude에게 "텔레그램_아카이브_계획.md 보고 이어서 하자" 라고 하면 됩니다.

---

## 1. 문제

증권사 리서치 PDF가 `C:\Users\infomax\Desktop\보따리\YYMMDD\` 에 매일 쌓입니다.
이걸 계속 로컬에만 둬도 되는지, 어디 올려서 어디서든 볼 수 있는지가 출발점이었습니다.

### 실측 (2026-08-05 기준)

| 항목 | 수치 |
|---|---|
| 날짜 폴더 | 50개 (260518 ~ 260805) |
| 전체 파일 | 2,494개 / **3.0 GB** |
| PDF만 | 2,393개 |
| 하루 평균 | 47파일 / **60 MB** |
| 연 환산 | **약 15 GB/년** |
| PDF 평균 크기 | 1.21 MB |
| **PDF 최대 크기** | **13.51 MB** (JPM Fixed Income) |
| 20MB 초과 파일 | **0개** ← 텔레그램 한도에 여유 |
| C: 남은 공간 | 44.9 GB (D:는 875 GB 여유) |

추가로 `C:\Users\infomax\Downloads` 에 PDF 801개(1.3GB), `Downloads\쓰레기통` 에 605개(1.3GB)가
더 있습니다. 보따리와 중복 가능성이 높아 별도 정리 대상.

---

## 2. 검토한 대안과 탈락 이유

### ❌ Vercel에 PDF 올리기

Vercel은 **Git 저장소를 통째로 배포**하는 구조라, PDF를 올리려면
`squarrel808/QAE-dashboard` 에 커밋해야 합니다.

- 현재 저장소 **29 MB** → PDF 넣으면 **3 GB + 매달 1.3 GB**
- Git은 **지운 파일도 히스토리에 영구 보존**. PDF는 델타 압축이 안 먹혀 원본 크기 그대로 누적
- GitHub 파일당 100MB 하드 차단 / 저장소 1GB 권장·5GB 경고 → **약 4개월 뒤 push 차단**
- 배포마다 전체 파일 재업로드 → 빌드 실패
- Hobby 플랜은 비상업적 용도 한정 (업무용이면 Pro $20/월)

> 참고: "클릭하면 다운로드받게 하면 되지 않나?" → **용량과 무관합니다.**
> 다운로드 링크든 인라인 뷰어든 파일 실체는 어느 서버엔가 있어야 하고,
> "Vercel에 둔다" = "git에 커밋한다" 이기 때문입니다.
> (`file:///C:/...` 로 로컬 파일 직접 링크하는 것도 브라우저가 보안상 차단합니다.)

### △ Cloudflare R2 — 정석이지만 보류

10GB 무료 + egress 무료, 서명 URL 기본 제공, 파일 크기 제한 없음.
**기술적으론 이쪽이 정석**이지만 10GB 초과 시 소액 과금이라 이번엔 보류.
→ 나중에 갈아탈 수 있음. `index.json` 구조만 유지하면 업로드 함수 하나만 교체하면 됨.

### ✅ 텔레그램 — 채택

저장 용량·대역폭 **전부 무료**, 사실상 무제한. 앱에서 검색·오프라인 저장까지 공짜.
단점은 비공식 용도라 보장이 없다는 점(→ 백업 아님, 아래 주의점 참조).

---

## 3. 목표 구조

```
수집 스크립트 → 텔레그램 비공개 채널 (PDF 원본 저장)
                      ↓
               index.json (날짜/제목/message_id/file_id/md5)  ← 이것만 git 커밋
                      ↓
            Vercel macro_hub "증권사 자료" 탭 (목록 + 다운로드 버튼)
```

**웹에서 클릭 시 동작:**
```
브라우저 클릭
  → Vercel API route /api/pdf/[id]
  → 텔레그램 getFile 호출 (토큰은 서버 환경변수)
  → 텔레그램 임시 URL로 302 리다이렉트
  → 브라우저가 텔레그램에서 직접 다운로드
```

핵심은 **302 리다이렉트**. 파일 바이트가 Vercel을 통과하지 않아
**Vercel 대역폭을 거의 안 쓰고 저장소는 29MB 유지**됩니다.

### Bot API 한도 (실측과 대조)

| | 한도 | 우리 실측 |
|---|---|---|
| 업로드 | 50 MB/파일 | 최대 13.51 MB ✅ |
| **다운로드** | **20 MB/파일** | 최대 13.51 MB ✅ |
| 총량 | 사실상 무제한 | 15 GB/년 ✅ |

⚠️ 하루치를 zip으로 묶어 올리면 20MB를 넘길 수 있으니 **파일 단위로 유지**할 것.

---

## 4. 재개 시 절차

### A. 사용자가 직접 (약 5분) — **여기서 멈춰 있음**

**1) 봇 만들기**
텔레그램에서 `@BotFather` → `/newbot` → 봇 이름 → username(반드시 `bot`으로 끝남, 예: `botari_archive_bot`)
→ **토큰** 발급

**2) 비공개 채널 생성**
New Channel → 이름(예: `연구자료 아카이브`) → **Private** 선택 (반드시 Private)

**3) 봇을 채널 관리자로 추가**
채널 → 이름 탭 → Administrators → Add Administrator → 봇 username 검색 → 추가
권한은 **Post Messages** 만 있으면 됨

**4) 채널에 아무 메시지나 하나 쓰기** (`test`)
⚠️ **반드시 3번 다음에.** 봇을 관리자로 넣기 전 메시지는 5번에서 안 잡힘

**5) chat_id 확인**
브라우저에서 `https://api.telegram.org/bot<토큰>/getUpdates`
→ `"channel_post"` → `"chat"` → `"id"` 의 **`-100`으로 시작하는 숫자** 복사
→ 비어 있으면(`"result":[]`) 3↔4 순서가 꼬인 것. 메시지 하나 더 쓰고 새로고침

**6) 환경변수 등록** (PowerShell)
```powershell
setx TELEGRAM_BOT_TOKEN "발급받은 토큰"
setx TELEGRAM_CHAT_ID   "-100으로 시작하는 숫자"
```
⚠️ `setx`는 **새로 여는 창부터** 적용 → 등록 후 **VSCode 완전히 껐다 켜기**
⚠️ 토큰은 채팅에 붙여넣지 말 것. 환경변수에만 두면 Claude가 읽어서 씀

**동작 테스트** (선택)
```
curl -F chat_id=%TELEGRAM_CHAT_ID% -F document=@"C:\Users\infomax\Desktop\보따리\260805\<아무파일>.pdf" ^
     -F caption="260805 테스트" https://api.telegram.org/bot%TELEGRAM_BOT_TOKEN%/sendDocument
```

### B. Claude가 만들 것

**7) 업로드 스크립트**
보따리 날짜폴더 스캔 → md5로 중복 제외 → 채널 업로드 → `index.json` 기록
- md5 함수는 `보따리\_자동화\daily_summary.py` 의 `md5()` (59행) 재사용
- 캡션에 날짜·원본 파일명을 박아 앱 검색으로 찾히게 할 것
- 파일 사이 1~2초 sleep, HTTP 429 오면 `retry_after` 만큼 대기 (flood wait 대응)

**8) 1건 테스트** — 채널에 파일 뜨는지 확인

**9) 기존 2,393개 소급 업로드** — 3GB라 회선 속도에 따라 **1~2시간**. 자기 전에 실행 권장

**10) 매일 자동 실행 등록** — 기존 `리서치요약` 작업처럼 `schtasks` 로 등록

**11) (선택) macro_hub "증권사 자료" 탭** — 날짜별 목록 + 다운로드 버튼 + 비밀번호 보호
- `macro_hub/app/equity/page.tsx` 의 JSON 읽는 패턴과 동일하게 붙이면 됨
- 현재 `master_dashboard.html:41` 의 "증권사 자료" 탭은
  `https://ibreport-dashboard.vercel.app` 를 iframe으로 띄우기만 함
  (`QAE/ibreport_telegram/` 폴더는 2026-07-07 이후 **빈 껍데기**)

---

## 5. 반드시 지킬 것

- 🔒 **채널은 무조건 Private, 초대 링크 배포 금지.** GS/JPM 리서치는 라이선스 자료
- 🔒 **`/api/pdf/[id]` 에 인증(middleware 비밀번호) 필수.** 안 걸면 URL 아는 사람 누구나 받아감
- 🔒 **토큰은 환경변수에만.** 현재 `.gitignore` 에 `*.pdf` 규칙이 없고 QAE는 이미 PDF를
  추적 중(Haver 가이드 2건)이라, 보따리를 QAE 안으로 옮기면 **그대로 커밋됨**
- ⚠️ **텔레그램은 백업이 아님.** SLA 없고 계정 정지/분실 시 통째로 소실.
  원본 아카이브는 D:(875GB 여유)에 두고 텔레그램은 "어디서든 꺼내보는 사본"으로

---

## 6. 아직 확인 못 한 것

- **`squarrel808/QAE-dashboard` 저장소가 Private 인지** — gh CLI가 설치돼 있지 않아 미확인.
  Public이면 PDF 이전에 이미 커밋된 데이터부터 점검 필요

---

## 7. 곁가지로 나온 정리 과제

- 보따리를 **D: 로 이전** 권장 (C: 여유 44.9GB, 연 15GB씩 증가).
  옮기면 `daily_summary.py:25` 의 `BASE` 한 줄 + 수집 스크립트 경로 수정 필요
- `Downloads` + `Downloads\쓰레기통` PDF **2.5 GB 정리 대상** (보따리와 중복 가능성 높음)
