/node_modules
/.next
/out
.env
.env.local
*.tsbuildinfo
next-env.d.ts

.vercel

# (변경) /public/report_files 는 이제 git에 올라감 — 대시보드에서 PDF 링크가 열리도록.
# ※ GitHub 는 파일당 100MB 제한이 있음. 그보다 큰 PDF가 생기면 push 가 거부되므로 그 파일만 지우면 됨.
